<resources>
    <!-- Depend on Material Components theme (available via material dependency). -->
    <style name="Theme.ModCompiler" parent="Theme.MaterialComponents.DayNight.NoActionBar" />
</resources>
